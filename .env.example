# Copy to .env and provide strong credentials before running docker compose.
POSTGRES_DB=sentinel
POSTGRES_USER=sentinel
POSTGRES_PASSWORD=change-me-please
POSTGRES_URL=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}
REDIS_URL=redis://localhost:6379/0
VAULT_ADDR=http://localhost:8200
SIGNING_KEY=change-me-dev-only
SENTINEL_TENANT_ID=tenant-dev
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
